apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Reusable Workflow
on:
  workflow_dispatch:
jobs:
  call_bar_workflow:
    uses: CP-QA-Org/Build-Repo/blob/main/.cloudbees/workflows/build-workflow.yaml