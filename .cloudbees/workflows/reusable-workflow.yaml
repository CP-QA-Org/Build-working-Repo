apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Reusable Workflow
on:
  workflow_dispatch:
jobs:
  call_bar_workflow:
    uses: CP-QA-Org/Build-Repo/.cloudbees/workflows/build-workflow.yaml
    secrets:
      GH_SECURITY_PAT: ${{ secrets.GH_SECURITY_PAT }}